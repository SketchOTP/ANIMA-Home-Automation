# 026A — owner connection and visual console handoff

Disposition: **CONTINUE — bounded owner deployment retained; full Goal incomplete**.

Latest extension adds Ring setup, sparse multi-signal evidence, owner initiative
settings, source-linked learned suggestions and scoped daily/multi-day review
dispatch. Deployed image edd377d00075 is healthy. See
`docs/RING-LEARNING-OWNER-INCREMENT-026A.md` for exact counts and boundaries.
Automatic SENTRY polling/TTS remains OFF; Ring account/event and phone sources
are not commissioned. Generated workflow installation remains beyond the
implemented proposal path. Tapo/Wansview remain paused. No hosted-CI claim.

Current owner extension: household/personal text preferences, long-term Obsidian
memory, phone-source presence, context-aware reasoning and purple/black UI.
The foundation is deployed; real resident SENTRY read and one guided note write
pass. See `docs/HOUSEHOLD-CONTEXT-AND-INITIATIVE-026A.md`. Automatic polling/TTS
remains OFF, no phone tracker exists yet, and router/phone commissioning plus
controlled autonomous event/voice qualification remain. This supersedes the
routines-only priority below without discarding its valid historical evidence.

Latest owner steer: Tapo/Wansview development **PAUSED**. Bounded Routines
completion is deployed on the Linux PC; guide and exact local validation:
`docs/FAMILY-ROUTINES-OWNER-GUIDE.md`. Owner can add a plain member and
create/filter/edit/enable/disable/remove routines with retained history. No
family schedules seeded; no automatic SENTRY wake/voice completion claimed.

Earlier 2026-09-07 UTC vendor continuation: see
`docs/VENDOR-SENTRY-CONTINUATION-026A.md` for the PC-local vendor receiver/status
deployment, final test counts and explicit live-source/SENTRY wake limitations.
The implementation-checkpoint counts and helper topology below are historical.
No Tapo/Wansview live delivery or automatic SENTRY speech is complete.

The owner can now open the graphical ANIMA console against their actual HA
household, discover inventory, create rooms and commission the two existing
SenseGuards into matching spaces. The separate scoped Codex worker has returned
a real read response to the browser. Fifteen management sections are connected
to existing bounded APIs; unsupported operations are not generic HA/code tools.

The latest bundle fixes the queued-response identifier, trusted HA invocation
context, room-tool execution classification, stable multi-tab CSRF, and
all-or-nothing dashboard refresh. Policy, identity and physical verification
remain Core-owned. Worker ambiguity is retained without automatic retries.

## Validation at the implementation checkpoint

- Full backend validation: 278 pytest tests, strict mypy on 79 source files,
  Ruff format/check: PASS.
- Client/worker suite: 47 tests and 43 subtests: PASS.
- OPA: 9/9 PASS. Python sdist/wheel and Docker UI build/start/health: PASS.
- Frontend final focused suite: 24 desktop browser scenarios, TypeScript,
  static tests (5/5) and Vite: PASS. The recurring device-count failure was
  reproduced on the old bundle before the partial-refresh correction.
- Earlier same-increment responsive matrix: 36/36. Fresh corrective-bundle
  PostgreSQL/OPA/Core H4 desktop 9/9 and H5V approval/rejection 2/2 passed.
  The preceding Cnn calendar failure is retained in EVIDENCE.md.
- Hosted CI runs the repository-wide integration/ARM64/browser/container/safety
  suite on the published SHA and uploads the owner JUnit metadata with its
  existing evidence artifact. Exact run/SHA/artifact results belong to the
  publication handoff and Notion, not a self-referential source-file hash.

## Runtime and limits

The corrected frontend build is `index-5yLOSLzO.js`, SHA-256
`972f3cabb48370f3b4ca59405f541bfb6c3bb204001e499c4ef50c1dd62e08e8`.
Deployment/served-asset verification is recorded separately from this build hash.
Its private connection and client files remain outside Git. The workstation
worker/tunnel services are separate from protected SENTRY and retain its tree.

Two failed room requests are negative evidence, not successful setup. Their
source-level routing defect is corrected and tested; a new live request must
check current state rather than replay either ambiguous turn. Upstream HA initially
had zero SenseGuard entities; one normal ZHA reload recovered twelve entities and
eight current-state entries. Physical monitoring/alert delivery remains unqualified
until the ANIMA binding and actual event path are exercised. Several management mutations are
UI-only until their SENTRY execution profiles are qualified. See the complete
bounded matrix and evidence in `docs/OWNER-VISUAL-CONSOLE-026A.md` and this packet.

No resident SENTRY voice/persona completion, arbitrary code-editing agent,
unrestricted HA administration or `ANIMA_HA_PROTOTYPE_GOAL_COMPLETE` is claimed.
Phases 0–14 acceptance is unchanged. No historical Phase 15 scenario phase was
started, and no new resilience scope was introduced.
