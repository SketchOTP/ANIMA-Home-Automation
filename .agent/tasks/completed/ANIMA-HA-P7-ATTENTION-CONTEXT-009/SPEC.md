# Specification — ANIMA-HA-P7-ATTENTION-CONTEXT-009

Implement deterministic local attention over already-journaled canonical events. Persist immutable decisions, guaranteed and aggregate reasoning triggers, processing cursor and suppression state transactionally. Build versioned sparse ContextPackets from bounded existing ANIMA sources while preserving provenance, uncertainty, trust and ANIMA-owned egress classifications. Replay must be read-only and side-effect free. Attention selects when future cognition runs; it never selects household actions. No Luna or Phase 8 behavior is authorized.
