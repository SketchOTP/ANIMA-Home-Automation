# Plan and disposition

1. Reconstruct accepted Phase 9 state from Notion, repository, GitHub, and live
   local-filesystem validation.
2. Compare Hatchet, Temporal, APScheduler, croniter, and PostgreSQL primary
   sources; record the decision in `.agent/EXTERNAL.md`.
3. Build the ANIMA-owned task/run model over existing PostgreSQL/Psycopg and
   wrap croniter narrowly.
4. Add task lifecycle tools and household-aware trusted-native invocation.
5. Emit deterministic guaranteed due events and bridge them into the existing
   Attention → Context → AgentRuntime path.
6. Validate unit, PostgreSQL, migration, replay, recovery, static, build, OPA,
   safety, and hosted CI evidence.
7. Publish implementation, then governed evidence closure, while keeping Phase
   11 unauthorized.
