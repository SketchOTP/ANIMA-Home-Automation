# Plan — Goal SENTRY text operation 025A

1. Add an ANIMA-owned ephemeral live-result boundary over existing PostgreSQL
   notification infrastructure.
2. Publish only after the durable Core result transition succeeds.
3. Add a household-scoped authenticated UI result route.
4. Update the conversation view to poll the request and display SENTRY's
   semantic response without an echo fallback.
5. Add deterministic bounded-result and household-isolation tests.
6. Run focused/full validation available in the known-good environment.
7. Record exact implementation and hosted governance evidence before closing
   the packet; keep Phase 15 unauthorized.
