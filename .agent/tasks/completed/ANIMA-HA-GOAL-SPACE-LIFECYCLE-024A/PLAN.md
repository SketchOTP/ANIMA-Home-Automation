# Plan — ANIMA-HA-GOAL-SPACE-LIFECYCLE-024A

1. Extend the canonical graph with household-scoped parent lookup, safe move,
   and history-preserving empty-place retirement.
2. Expose typed `move_space` and `remove_space` capabilities through the
   existing trusted invocation context and Core UI gateway.
3. Return parent identity in the household-place read model.
4. Add explicit owner controls for rename, move, and remove in the Spaces view.
5. Validate backend, static, frontend, and diff contracts.
6. Publish documentation and a completed evidence packet after exact-head
   validation.
